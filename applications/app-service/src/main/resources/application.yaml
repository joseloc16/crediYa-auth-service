##Spring Configuration
server:
  port: 8080
spring:
  application:
    name: Auth-Service
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: /h2
  profiles:
    include:
